import React, {Component, Fragment} from "react";
import {connect} from "react-redux";

class HardDevice extends Component{
    render() {
        return (
            <Fragment>

            </Fragment>
        )
    }
}
const mapStateToProps = (state) => {
    return{

    }
}
const mapDispatchToProp = (dispatch) => {
    return{

    }
}
export default connect(mapStateToProps, mapDispatchToProp)(HardDevice);
